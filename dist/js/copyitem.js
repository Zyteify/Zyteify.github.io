"use strict";
/* import { cloneDeep } from 'lodash';
// Standalone function to copy an object into the Item class
function copyObjectToItem(obj: object): Item {
    const item = new Item();
    const copiedObject = cloneDeep(obj);
    Object.assign(item, copiedObject);
    return item;
  } */ 
